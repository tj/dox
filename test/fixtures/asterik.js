/**
 * One
 */
var one = function() {
    return ['**/**', '*/*', '*/**', '**/*'].join('');
}

/**
 * Two
 * @param {number} num
 */
var two = function(num) {
    return num * 2;
}

/**
 * Three
 */
let three = function() {
    return ['**/**', '*/*', '*/**', '**/*'].join('');
}

/**
 * Four
 * @param {number} num
 */
const four = function(num) {
    return num * 2;
}
