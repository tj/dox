/**
 * only
 *
 * @sample
 * one
 * two
 * three
 */
function only() {
}

/**
 * first
 *
 * @sample
 * one
 * two
 * three
 * @bar last
 */
function first() {
}

/**
 * last
 *
 * @foo first
 * @sample
 * one
 * two
 * three
 */
function last() {
}

/**
 * mid
 *
 * @foo first
 * @sample
 * one
 * two
 * three
 * @bar last
 */
function mid() {
}

/**
 * only
 *
 * @param {String} foo
 * one
 * two
 * three
 */
function onlyParam() {
}

/**
 * first
 *
 * @param {String} foo
 * one
 * two
 * three
 * @bar last
 */
function firstParam() {
}

/**
 * last
 *
 * @foo first
 * @param {String} foo
 * one
 * two
 * three
 */
function lastParam() {
}

/**
 * mid
 *
 * @foo first
 * @param {String} foo
 * one
 * two
 * three
 * @bar last
 */
function midParam() {
}

/**
 * only
 *
 * @return {String}
 * one
 * two
 * three
 */
function onlyReturn() {
}

/**
 * first
 *
 * @return {String}
 * one
 * two
 * three
 * @bar last
 */
function firstReturn() {
}

/**
 * last
 *
 * @foo first
 * @return {String}
 * one
 * two
 * three
 */
function lastReturn() {
}

/**
 * mid
 *
 * @foo first
 * @return {String}
 * one
 * two
 * three
 * @bar last
 */
function midReturn() {
}

/**
 * example
 *
 * @example
 *     test(one);
 *     test(two);
 */
function example() {
}

/**
 * only
 *
 * @test {ok} passed test ok
 * one
 * two
 * three
 */
function onlyTest() {
}

/**
 * first
 *
 * @test {} this test has
 * not been
 * implemented yet
 * @bar last
 */
function firstTest() {
}

/**
 * last
 *
 * @foo first
 * @test {pending} test to be
 * implemented soon
 */
function lastTest() {
}

/**
 * mid
 *
 * @foo first
 * @test {ok} passing
 * one
 * two
 * three
 * @bar last
 */
function midTest() {
}
