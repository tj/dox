h1= filename.split('/').slice(-1)[0]
.filename= filename

#comments
  - each comment in comments
    - if (comment.isPrivate || comment.ignore) continue
    .comment
      .summary!= comment.description.summary
      code
        pre!= comment.code